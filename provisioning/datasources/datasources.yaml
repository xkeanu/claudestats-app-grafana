apiVersion: 1

datasources:
  - name: Claude Stats Remote
    type: prometheus
    uid: claude-stats-remote
    access: proxy
    url: http://162.55.221.167:9090
    isDefault: true
    editable: true
    jsonData:
      httpMethod: POST
      manageAlerts: true
      prometheusType: Prometheus

  - name: Prometheus Local
    type: prometheus
    uid: prometheus-local
    access: proxy
    url: http://prometheus:9090
    isDefault: false
    editable: false
    jsonData:
      httpMethod: POST
      manageAlerts: true
      prometheusType: Prometheus
